{
  "name": "Backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "migrate": "dotenv -- cross-var \"xcopy /e /s %LOCAL_PATH%  %MIGRATE_PATH%\"",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "nodemon --watch src -e ts --exec ts-node -r dotenv/config src/index.ts",
    "start": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-var": "^1.1.0",
    "dotenv-cli": "^5.1.0",
    "express": "^4.17.3",
    "node-postgres": "^0.6.2",
    "pg": "^8.7.3",
    "postgres-migrations": "^5.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.25",
    "@types/pg": "^8.6.5",
    "dotenv": "^16.0.0",
    "nodemon": "^2.0.15",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
